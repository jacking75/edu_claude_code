# Claude Code 출력을 복사·저장하는 방법 정리 (`/copy`, `/export`)


**`/copy` 커맨드란:**

`/copy`는 Claude Code의 응답을 클립보드에 복사하기 위한 빌트인 커맨드다. v2.1.72에서 파일 출력 기능(`w` 키)과 인덱스 지정(`/copy N`)이 추가되었다.

주요 기능:

- 최신 응답을 클립보드에 복사
- 코드 블록이 포함된 경우, 개별 블록을 선택 가능
- `w` 키로 파일에 직접 출력
- `/copy N`으로 N번째로 새로운 응답을 지정

그럼 각 기능을 순서대로 시험해보겠다.

---

**시험해보기:**

**환경**

- Claude Code v2.1.87
- macOS

**1. 기본 `/copy` (최신 응답 복사)**

가장 심플한 사용법이다. Claude Code가 응답한 후에 `/copy`라고 입력하기만 하면, 직전의 응답이 클립보드에 복사된다.

복사된 내용은 Markdown 형식의 텍스트다. 그대로 에디터나 Slack에 붙여넣어 사용할 수 있다. 응답에 코드 블록이 포함되지 않은 경우에는, 피커를 거치지 않고 즉시 클립보드에 복사된다.

**2. 코드 블록 피커 (개별 선택)**

응답에 코드 블록이 포함되어 있는 경우, `/copy`를 실행하면 인터랙티브한 피커 UI가 표시된다.

예를 들어, 3개의 코드 블록을 포함한 응답에 대해 `/copy`를 실행하면 화살표 키로 선택하고, Enter로 확정한다. "Full response"를 선택하면 응답 전체가, 개별 코드 블록을 선택하면 해당 블록의 내용만 복사된다.

긴 응답 중에서 "이 코드 블록만 갖고 싶다"는 상황에서, 수동으로 범위를 선택할 필요가 없어진다.

**3. `w` 키로 파일 출력**

피커 UI 표시 중에 `w` 키를 누르면, 클립보드가 아닌 파일에 직접 출력할 수 있다.

```
❯ 1. Full response           ← 여기서 w키를 누른다

Written to /var/folders/.../claude/response.md
```

임시 디렉터리에 Markdown 파일로 저장된다. 클립보드를 거치지 않고 파일로 남길 수 있기 때문에, 나중에 다른 툴로 열거나 그대로 보관해두고 싶은 경우에 활용할 수 있을 것 같다.

**4. `/copy N`으로 과거 응답 지정**

`/copy N`으로 N번째로 새로운 응답을 지정할 수 있다 (v2.1.72 이후).

```
> /copy 1    # 최신 응답 (/copy와 동일)
> /copy 2    # 2개 전 응답
> /copy 3    # 3개 전 응답
```

"방금 전 회답이 아니라, 그 전 회답이 갖고 싶다"고 할 때, 대화를 거슬러 올라가 마우스로 선택할 필요가 없어진다.

참고로, 거슬러 올라갈 수 있는 것은 최근 20건까지인 것 같다. 그 이상 이전의 응답이 필요한 경우에는 `/export`로 대화 전체를 저장하는 방법을 사용할 수 있을 것 같다.

**5. `/export`로 대화 전체를 출력**

`/export`는 세션 전체의 대화를 플레인 텍스트로 출력하는 빌트인 커맨드다. `/copy`가 특정 응답을 대상으로 하는 것에 비해, 이쪽은 대화 전체가 대상이다.

파일 이름을 지정하지 않는 경우에는 다이얼로그가 표시되고, 클립보드에 복사할지 파일에 저장할지 선택할 수 있다.

파일 이름을 지정하면 직접 저장도 가능하다. 단, 확장자를 `.md`로 해도 `.txt`로 저장되었다. 플레인 텍스트 형식의 출력으로 고정되어 있는 것 같다.

---

**`/copy`와 `/export` 사용 구분:**

| 관점 | `/copy` | `/export` |
|---|---|---|
| 대상 | 특정 응답 1개 | 세션 전체의 대화 |
| 출력 형식 | Markdown (응답 그대로) | 플레인 텍스트 (트랜스크립트) |
| 코드 블록 선택 | 개별로 선택 가능 | 불가 (전체 출력) |
| 파일 출력 | 피커에서 `w` 키 → 임시 파일 | 파일 이름 지정으로 직접 저장 |
| 주요 용도 | 코드 조각 전사, 회답 공유 | 대화 로그 저장, 되돌아보기 |

**사용 구분 기준:**

- Claude의 회답을 Slack이나 문서에 붙여넣고 싶을 때 → **`/copy`**
- 코드 블록만 추출하고 싶을 때 → **`/copy`** (피커에서 선택)
- 코드 블록을 파일에 저장하고 싶을 때 → **`/copy`** → **`w` 키**
- 몇 턴 전의 회답이 갖고 싶을 때 → **`/copy N`**
- 세션 전체의 기록을 남기고 싶을 때 → **`/export`**
- 대화 내용을 파일에 저장하고 싶을 때 → **`/export filename`** (`.txt`로 저장됨)  