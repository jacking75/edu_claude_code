# 사내 Claude Code 스터디
  
[출처](https://zenn.dev/canly/articles/cc0891517e45cc )  


## **개요:**

| 항목 | 내용 |
|------|------|
| 대상자 | Claude Code 초급자~중급자 (기술 레벨·스택 혼재) |
| 형식 | 강의식 (스크린샷으로 보충) |
| 시간 | 약 90분 |
| 전제 | Claude Code 도입 완료, 기본 조작은 기지(旣知) |

---

## **구성:**

| Part | 내용 | 본편 | Q&A | 합계 |
|------|------|------|-----|------|
| 1 | Claude Code 기초 | 10분 | 2분 | 12분 |
| 2 | 컨텍스트 윈도우 운용 | 10분 | 2분 | 12분 |
| 3 | 확장 기능 | 15분 | 2분 | 17분 |
| 4 | 병렬 처리 | 12분 | 2분 | 14분 |
| 5 | 피드백 루프 | 10분 | 2분 | 12분 |
| 6 | SDD (사양 주도 개발) | 5분 | 2분 | 7분 |
| 7 | Team 플랜 환경 | 10분 | 2분 | 12분 |
| 8 | 정리 | 5분 | - | 5분 |

```
【기초】→【확장】→【응용】→【보충】

Part 1-2: Claude Code를 올바르게 이해한다
Part 3-4: 커스터마이즈 수단을 안다
Part 5-6: 실전에서 활용한다
Part 7-8: Team 플랜 환경 + 정리
```

---

## **Part 1: Claude Code 기초 (10분 + Q&A 2분)**

## **1.1 `CLAUDE.md`의 역할과 작성법:**

### **`CLAUDE.md`란**

프로젝트의 루트(또는 임의의 디렉터리)에 두는 **Markdown 형식의 설정 파일**이다. Claude Code가 매번 자동으로 읽어들여, 프로젝트 고유의 지시로서 활용한다.

### **배치 위치와 읽기 순서:**

```
~/.claude/CLAUDE.md          # 유저 레벨 (전체 프로젝트 공통)
./CLAUDE.md                  # 프로젝트 루트
./.claude/CLAUDE.md          # 프로젝트 루트 (위와 동일한 동작)
./src/CLAUDE.md              # 서브디렉터리 (해당 디렉터리 이하에서 유효)
```

**Note**: `./CLAUDE.md`와 `./.claude/CLAUDE.md`는 동일한 동작을 한다. `.claude/`에 설정 파일을 모아두고 싶다면 후자를 사용한다.  
**읽기 순서**: 유저 레벨 → 프로젝트 루트 → 서브디렉터리 (나중에 읽힌 것이 우선)

> 📎 출처: [Claude Code Docs - 메모리](https://code.claude.com/docs/ja/memory)

### **작성해야 할 내용:**

| 카테고리 | 예시 |
|---------|------|
| 프로젝트 개요 | "이 프로젝트는 Next.js + TypeScript의 Web앱" |
| 코딩 규약 | "함수는 화살표 함수로 작성", "주석은 일본어" |
| 금지 사항 | "any는 사용하지 않는다", "console.log는 커밋하지 않는다" |
| 파일 구성 | "src/components/에 UI 컴포넌트" |
| 자주 쓰는 커맨드 | "테스트는 `npm test`", "빌드는 `npm run build`" |

### **작성 요령:**

```markdown
# 프로젝트 개요
Next.js 14 + TypeScript + Prisma로 구축된 EC 사이트

# 코딩 규약
- 함수는 화살표 함수로 통일
- 타입 정의는 `types/` 디렉터리에 집약
- 컴포넌트는 named export 사용

# 금지 사항
- `any` 타입 사용 금지
- `console.log` 커밋 금지

# 테스트
테스트 실행: `npm test`
테스트 파일은 `__tests__/`에 배치
```

**포인트:**
- 간결하게, 목록 형식으로
- Claude가 이해하기 쉬운 명확한 지시
- 너무 길면 매번 토큰 소비가 늘어난다 (후술)

---

## **1.2 `.claude/rules/` (프로젝트 룰):**

### **프로젝트 룰이란**

`CLAUDE.md`를 **여러 파일로 분리**하여 관리하는 구조다. 프로젝트가 커지면 `CLAUDE.md`가 비대해지기 때문에, 토픽별로 파일을 나눌 수 있다.

> ⚠️ **대응 버전**: Claude Code 2.0.64 이후 (2025년 12월 10일 릴리스)부터 사용 가능

### **배치 위치:**

```
.claude/rules/
├── coding-style.md      # 코딩 규약
├── testing.md           # 테스트 방침
├── api-design.md        # API 설계 룰
└── security/            # 서브디렉터리도 가능
    └── auth.md
```

### **paths 지정의 중요성:**

YAML 프론트매터로 `paths`를 지정하면, 특정 파일 패턴에만 룰을 적용할 수 있다.

```yaml
---
paths:
  - src/api/**/*.ts
  - src/services/**/*.ts
---

# API 설계 룰
- 에러 핸들링은 반드시 try-catch로 감싼다
- 응답은 통일 포맷을 사용한다
```

**중요: paths 지정과 초기 컨텍스트 소비**

| paths 지정 | 읽기 타이밍 | 초기 컨텍스트 |
|-----------|-----------|------------|
| 없음 | **세션 시작 시에 읽기** | 소비한다 |
| 있음 | **해당 경로의 파일을 읽고 쓸 때** 읽기 | 소비하지 않는다 |

paths 지정이 없는 경우, 해당 룰은 **세션 시작 시에 자동으로 읽혀** 초기 컨텍스트를 소비한다. 룰이 늘어날수록, 아무것도 하지 않아도 토큰이 소비된다. 반면, paths 지정이 있는 룰은 **해당 경로의 파일에 대해 Read/Edit/Write 등의 툴이 실행되었을 때** 동적으로 읽혀진다. 사용하지 않는 룰로 컨텍스트를 압박하지 않기 때문에, 많은 룰을 정의해도 효율적이다.

**베스트 프랙티스**: 특정 디렉터리나 파일 종류에만 사용하는 룰에는 반드시 `paths`를 지정한다.

### **정리:**

`CLAUDE.md`에 모든 것을 우겨넣는 게 아니라, **룰을 적절히 분리**하고, **paths를 지정**하는 것이 중요하다. 이를 통해 필요할 때 필요한 룰만 읽혀, 컨텍스트를 효율적으로 사용할 수 있다.

> 📎 출처: [Claude Code Docs - 메모리](https://code.claude.com/docs/ja/memory)

---

## **1.3 설정 파일 배치 위치:**

### **3계층 구조:**

| 레벨 | 배치 위치 | 용도 |
|------|----------|------|
| 프로젝트 | `.claude/` | 프로젝트 고유 설정 |
| 유저 | `~/.claude/` | 개인의 전체 프로젝트 공통 설정 |
| 엔터프라이즈 | 관리자 설정 | 조직 전체 정책 |

### **주요 설정 파일:**

```
.claude/
├── settings.json        # 프로젝트 설정
├── settings.local.json  # 로컬 설정 (.gitignore)
├── rules/               # 프로젝트 룰
├── commands/            # 커스텀 커맨드
├── skills/              # 스킬
├── agents/              # 서브에이전트
└── output-styles/       # 출력 스타일
```

> 📎 출처: [Claude Code Docs - 설정](https://code.claude.com/docs/ja/settings)

---

## **1.4 입출력 흐름:**

### **Claude Code의 처리 플로우:**

```
┌─────────────────────────────────────────────────────┐
│  입력                                               │
│  ┌───────────┐  ┌───────────┐  ┌───────────┐       │
│  │ 프롬프트  │ + │ CLAUDE.md │ + │  rules/   │       │
│  └───────────┘  └───────────┘  └───────────┘       │
└─────────────────────────────────────────────────────┘
                      ↓
┌─────────────────────────────────────────────────────┐
│  Claude Code (LLM)                                  │
│  - 툴 실행 (파일 읽고 쓰기, 커맨드 실행 등)             │
│  - 필요에 따라 스킬/서브에이전트 호출                   │
└─────────────────────────────────────────────────────┘
                      ↓
┌─────────────────────────────────────────────────────┐
│  출력                                               │
│  - 코드 생성·편집                                    │
│  - 커맨드 실행 결과                                   │
│  - 설명·제안                                         │
└─────────────────────────────────────────────────────┘
```

※ 간략도. 실제로는 스킬·MCP·출력 스타일 등도 상황에 따라 읽혀진다.  
**중요**: `CLAUDE.md`나 `rules/`의 내용은 **매번의 요청에 포함된다**. 즉, 설정이 많을수록 토큰을 소비한다. 사용 중인 토큰은 `/context`로 확인 가능하다.

커스터마이즈한 상태 라인을 설정해두면 알기 쉽다.

> 📎 출처: [Claude Code 베스트 프랙티스](https://www.anthropic.com/engineering/claude-code-best-practices)

---

## **1.5 허가 프롬프트에서의 Tab 조작:**

### **허가 프롬프트란**

Claude Code가 파일 편집이나 커맨드 실행 등 중요한 조작을 수행하기 전에 표시되는 **Yes/No 확인 다이얼로그**다.

### **Tab 키로 추가 지시:**

허가 프롬프트에서 **Tab 키를 누르면 추가 지시를 입력**할 수 있다. 승인(Yes)이든 거부(No)이든, 단순 선택이 아닌 추가 지시를 줄 수 있다.

```
Do you want to make this edit to README.md?
  > 1. Yes, 유효성 검사용 함수도 추가해  ← Tab으로 입력 중
    2. Yes, allow all edits during this session (shift+tab)
    3. No
```

### **유스케이스:**

| 상황 | 입력 예시 |
|------|---------|
| 승인 후 추가 변경 요청 | `Yes, [Tab] 다른 파일에도 같은 변경을 적용해` |
| 승인 후 수정 지시 | `Yes, [Tab] 10번째 줄의 오타도 수정해` |
| 승인 후 조건 추가 | `Yes, [Tab] 에러 핸들링도 넣어` |
| 거부 후 대안 지시 | `No, [Tab] 대신 이 파일은 변경하지 말고 새 파일을 만들어` |
| 거부 후 방침 변경 | `No, [Tab] 이 방법이 아닌 ○○ 접근법으로 구현해` |

부분적으로 맞고 부분적으로 틀린 제안에 대해, 전체를 승인/거부하는 게 아니라 추가 지시로 조정할 수 있다.

> 📎 출처: [bcherny's tweet](https://x.com/bcherny/status/2011332893772308867)

---

## **1.6 제공해야 할 컨텍스트:**

### **파일 경로:**

`@path/to/file` 형식으로 전달하면, 해당 파일을 컨텍스트로 제공할 수 있다.

```
> @README.md 에 중복된 표현이 없는지 확인해
```

### **이미지:**

복사&붙여넣기나 드래그&드롭으로 멀티모달 입력을 활용할 수 있다.

```
 [ Image #1] (↑ to select)
> 이 버튼 색상을 빨간색으로 바꿔줘
```

### **Claude에게 질문시키기 & AskUserQuestion 툴:**

빠뜨림을 없애기 위해, Claude에게 지시를 주는 것뿐만 아니라, Claude에게 질문을 시키는 것도 유효하다. 그 때는 `AskUserQuestion` 툴을 사용하게 하면 편리하다.

```
> 사용자 인증 신기능을 구현하려고 합니다. 사양을 정하고 싶으니, AskUserQuestion 툴을 사용해서 질문해줘.
```

**AskUserQuestion 툴이란, 실행 중에 유저에게 질문을 제시**하는 툴이다.

**질문 형식:**
- 2~4개의 선택지를 제시
- 멀티 셀렉트 대응 (복수 선택 가능)
- "기타"로 자유 입력 가능

**`CLAUDE.md`에서의 활용:**  
명시적으로 호출하지 않아도 사용되는 경우가 있지만, `CLAUDE.md`에 지시를 작성해두면 적극적으로 `AskUserQuestion` 툴을 사용해 질문하도록 할 수 있다.

```markdown
# 대화 방침
- 애매한 지시가 있으면, 구현 전에 AskUserQuestion으로 확인한다
- 여러 구현 방법이 있는 경우, 선택지를 제시하여 유저가 선택하도록 한다
```

---

## **Part 2: 컨텍스트 윈도우 운용 (10분 + Q&A 2분)**

## **2.1 토큰의 구조:**

### **토큰이란**

LLM이 처리하는 텍스트의 최소 단위다. 단어나 문자의 일부가 토큰으로 분할된다.

### **일본어 vs 영어:**

| 언어 | 같은 의미의 문장 | 토큰 수 |
|------|--------------|--------|
| 영어 | Canly Inc. has the mission of "creating a global infrastructure that supports store management," and provides "Canly," a comprehensive management service for MEO measures and Google Maps. | 36 토큰 |
| 일본어 | 株式会社カンリーは「店舗経営を支える世界的なインフラを創る」というミッションを掲げ、MEO対策・Googleマップの一括管理サービス「Canly（カンリー）」の提供を行っています。 | 55 토큰 |

**일본어는 영어보다 토큰 소비가 많다**. 이는 비용과 처리 시간에 직결된다.

> 📎 참고: [OpenAI Tokenizer](https://platform.openai.com/tokenizer)

### **토큰 증가의 영향:**

토큰 수가 늘어나면:
- **처리 시간이 증가하는 경우가 있다**
- **정확도가 저하됨** (Lost in the middle 등, 후술)
- **auto-compact에 의한 문맥 손실** (컨텍스트 상한에 도달하면 자동 압축되어 지시를 잊는다. 2.3에서 상술)
- **Bedrock (종량 과금)**: 비용이 증가, 후술
- **Team 플랜 (구독)**: Rate Limit에 도달하기 쉬워진다

---

## **2.2 컨텍스트 윈도우란?:**

### **Claude의 "작업 기억"**

컨텍스트 윈도우는 Claude가 **한 번에 처리할 수 있는 정보량의 상한**이다.

- 대화 이력
- 읽어들인 파일
- 툴 호출 결과
- `CLAUDE.md` / 시스템 프롬프트

→ 이들의 합계에 상한이 있다.

### **상한에 도달하면? (auto-compact):**

컨텍스트 윈도우의 **95%**를 넘으면, `auto-compact`가 자동 발동된다. 대화 이력이 요약·압축되어 작업을 계속할 수 있게 된다.  
**주의 사항:**
- 압축 시에 **상세 정보가 손실될** 가능성이 있다
- 지시나 코드의 일부가 "잊혀지는" 경우도 있다
- **수동으로 `/compact`나 `/clear`를 사용하는 편이 제어하기 쉽다**

**auto-compact를 끄는 설정:**

```json
// Claude Code 상에서 설정 (권장)
> /status → Auto-compact → false 선택

// 또는 settings.json 에서 설정
{
  "autoCompact": false
}
```

> 💡 **베스트 프랙티스**: auto-compact에 의존하지 말고, 자주 `/clear`나 새 세션으로 구분짓는다. 또한 지시를 거슬러 올라가 다른 지시로 바꾸고 싶은 경우는 `/rewind`를 사용한다.

---

## **2.3 토큰 증가의 부작용 (눈덩이식):**

### **LLM API는 스테이트리스(stateless)**

중요한 사실: **LLM API는 스테이트리스**다. 즉, 대화 이력을 API 측에서 보유하고 있지 않다.

```
요청1: [메시지1] → 응답1
요청2: [메시지1 + 응답1 + 메시지2] → 응답2
요청3: [메시지1 + 응답1 + 메시지2 + 응답2 + 메시지3] → 응답3
```

**매번, 전체 이력을 재전송하고 있다**. 이것이 토큰 소비가 "눈덩이식"으로 늘어나는 이유다.

> 📎 출처: [OpenAI - Conversation state](https://platform.openai.com/docs/guides/conversation-state)

### **부작용1: 정확도 저하**

#### Lost-in-the-middle 현상

긴 컨텍스트의 **중간 부분이 무시되기 쉽다**. LLM은 처음과 마지막의 정보를 중시하는 경향이 있다.

> 📎 출처: [Lost in the Middle 논문](https://arxiv.org/abs/2307.03172)

### **부작용2: Rate Limit 도달 가속화**

Team 플랜/구독에서는, 일정 시간 내 토큰 사용량에 제한이 있다.

```
컨텍스트가 비대해짐
    ↓
1 요청당 토큰 소비가 증가
    ↓
같은 횟수의 대화에서도 Rate Limit에 빨리 도달
    ↓
"사용량 상한에 도달했습니다"로 작업 중단 (혹은 종량 과금으로 전환되어 비용 증가)
```

### **부작용3: 비용 증가**

Bedrock 등의 종량 과금에서는, 토큰 수가 그대로 비용에 반영된다.  
**Claude Opus 4.5의 요금** (Bedrock):

| 종류 | 요금 |
|------|------|
| 입력 | $5 / 100만 토큰 |
| 출력 | $25 / 100만 토큰 |

**구체적인 예**: 같은 10번의 대화도, 컨텍스트 사이즈에 따라 크게 달라진다.

| 시나리오 | 입력 토큰 소비 | 입력 비용 |
|---------|-------------|---------|
| 경량 (10K × 10회) | 100K 토큰 | 약 $0.50 |
| 비대화 (50K × 10회) | 500K 토큰 | 약 $2.50 |
| 방치 (100K × 10회) | 1M 토큰 | 약 $5.00 |

**하루 작업으로 계산하면**: 100번의 대화라면, 경량 vs 방치로 **$5 vs $50**의 차이가 난다.

> 📎 출처: [Amazon Bedrock Pricing](https://aws.amazon.com/bedrock/pricing/)

### **부작용4: auto-compact에 의한 문맥 손실**

컨텍스트 윈도우의 **95%를 넘으면**, **auto-compact**가 자동 발동되어 대화 이력이 요약·압축된다.  
**압축 시에 손실되기 쉬운 정보:**

| 손실되기 쉬운 것 | 남기 쉬운 것 |
|--------------|-----------|
| 초기의 세부 지시 | 최근의 대화 |
| 수정한 코드의 상세 | 대략적인 태스크 목적 |
| "~하지 말 것" 등의 금지 사항 | 마지막에 언급한 내용 |

**구체적인 예:**

```
당신: "TypeScript로, 에러 핸들링을 꼼꼼하게 작성해"
Claude: (지시대로 구현)
# ... 긴 대화 ...
# ... auto-compact 발동 ...
당신: "다음 함수도 같은 방침으로"
Claude: (JavaScript로, 에러 핸들링 없이 구현) ← 지시를 잊어버렸다
```

**중요**: Lost in the middle은 "희미해지는" 것이지만, auto-compact는 "사라지는" 것이다. 더 심각한 영향이 있다.

---

## **2.4 대책: `/clear`, `/compact`, 새 세션:**

### **`/clear` - 대화 이력 초기화**

대화 이력을 완전히 리셋한다. 새로운 태스크를 시작하기 전에 실행하면 효과적이다.

### **`/compact` - 이력을 요약하여 압축**

지금까지의 대화를 요약하여 토큰 수를 줄인다. 문맥은 유지하면서 컨텍스트를 슬림화한다.  
**인수(argument)로 지시를 추가할 수 있다:**

```
> /compact TypeScript의 타입 정의에 관한 논의를 중점적으로 남겨줘
```

인수를 전달하면, 압축 시에 **무엇을 중시하여 남길지**를 지시할 수 있다. 중요한 문맥이 손실되는 것을 방지하는 데 유효하다.

### **새 세션:**

터미널을 새로 열어 `claude`를 실행한다. 가장 깨끗한 상태로 시작할 수 있다.

### **구분 사용 기준:**

| 상황 | 권장 액션 |
|------|---------|
| 새로운 태스크를 시작할 때 | `/clear` 또는 새 세션 |
| 긴 대화 도중 무거워졌을 때 | `/compact` |
| 전혀 다른 작업으로 이동할 때 | 새 세션 |
| 디버그가 제자리를 돌고 있을 때 | `/clear` 후 처음부터 |

> 📎 출처: [Claude Code Docs - 슬래시 커맨드](https://code.claude.com/docs/ja/slash-commands)

---

## **2.5 Plan 모드:**

### **Plan 모드란**

**계획을 세운 뒤에 실행**하는 모드다. 복잡한 태스크에서 정확도를 높이는 데 효과적이다.

### **사용법:**

```
Shift+Tab  # Plan 모드 전환
> /plan    # 커맨드로도 가능
```

또는 프롬프트에 "먼저 계획을 세워"라고 지시한다.

### **Plan 모드의 흐름:**

1. 태스크 분석 (파일 탐색, 코드 조사 등)
2. 실행 계획 제시 (파일 변경, 커맨드 실행 등)
3. 유저의 승인 (아래 선택지에서 선택)
4. 계획에 따라 실행

### **승인 시 선택지:**

계획이 완성되면, 다음 선택지가 표시된다.

| 선택지 | 동작 |
|--------|------|
| **Yes, clear context and auto-accept edits** | 컨텍스트를 초기화하고 자동 실행 |
| Yes, auto-accept edits | 그대로 자동 실행 |
| Yes, manually approve edits | 편집마다 승인하면서 실행 |
| Type here to tell Claude what to change | 계획 수정을 지시 |

**"Yes, clear context and auto-accept edits"의 효과:**  
Plan 모드에서는 계획을 세우기 위해 많은 파일을 읽어들이거나 코드를 조사하거나 한다. 이러한 **조사에서 쌓인 정보는 노이즈가 되기 쉽다**. 이 선택지를 선택하면:
- 조사 중에 읽어들인 파일 내용 등을 초기화
- **계획만 남기고** 최소한의 컨텍스트로 실행 시작
- Part 2.3에서 설명한 "컨텍스트 비대화"를 방지할 수 있다

```
┌─────────────────────────────────────────────┐
│  Plan 모드 중의 컨텍스트                       │
│  · 조사에서 읽은 파일 (다수)                    │
│  · 시행착오의 대화                              │
│  · 최종 계획 ← 이것만 있으면 실행 가능           │
└─────────────────────────────────────────────┘
         ↓ Yes, clear context... 선택
┌─────────────────────────────────────────────┐
│  실행 시작 시의 컨텍스트                        │
│  · 최종 계획만 ← 경량!                         │
└─────────────────────────────────────────────┘
```

**베스트 프랙티스**: 기본적으로 "Yes, clear context..."를 선택해도 된다. 계획에 필요한 정보는 요약되어 인계되므로, 조사 내용이 사라지는 걱정은 없다.

### **언제 사용하는가:**

| 상황 | Plan 모드 |
|------|---------|
| 여러 파일에 걸친 변경 | ○ 권장 |
| 대규모 리팩토링 | ○ 권장 |
| 새 기능 구현 | ○ 권장 |
| 간단한 버그 수정 | △ 불필요한 경우도 있음 |
| 질문에 대한 답변 | × 불필요 |

> 📎 출처: [Claude Code 베스트 프랙티스](https://www.anthropic.com/engineering/claude-code-best-practices)

---

## **Part 3: 확장 기능 (15분 + Q&A 2분)**

## **3.1 커스터마이즈 수단 전체 개요:**

Claude Code에는 여러 커스터마이즈 수단이 있다. 각각의 역할과 구분 사용법을 이해하자.

### **비교표:**

| 기능 | 트리거 | 로컬 배치 | 플러그인 배치 | 주요 용도 |
|------|--------|----------|------------|---------|
| 슬래시 커맨드 | 수동 `/cmd` | `.claude/commands/` | `commands/` | 정형 태스크 실행 |
| 스킬 | 자동 감지 | `.claude/skills/` | `skills/` | 특정 도메인 지식 제공 |
| 서브에이전트 | 자동 위임 | `.claude/agents/` | `agents/` | 전문 태스크 분담 |
| Hooks | 이벤트 구동 | `.claude/settings.json` | `hooks/hooks.json` | 자동 체크·변환 |
| MCP | 외부 연계 | `settings.json` / `.mcp.json` | `.mcp.json` | 외부 서비스와 통합 |
| 출력 스타일 | `/output-style` | `.claude/output-styles/` | `output-styles/` | 출력 형식 커스터마이즈 |
| GitHub Actions | PR/Issue | GitHub 설정 | - | 자동 코드 리뷰, 자동 구현 |
| 마켓플레이스 | `/plugin` | - | 설정 | 플러그인 배포·공유 |

### **네임스페이스 차이:**

로컬과 플러그인에서는 호출 시 이름이 다르다.

| 종류 | 로컬 | 플러그인 |
|------|------|--------|
| 커맨드 | `/review` | `/plugin-name:review` |
| 스킬 | `/security-audit` | `/plugin-name:security-audit` |
| 에이전트 | 자동 선택 | 자동 선택 (내부에서 식별) |

플러그인에서 설치한 기능은, **플러그인 이름이 접두사로 붙는다**.

### **선택 방법 플로우차트:**

```
무엇을 하고 싶은가?
    │
    ├─ 정형 작업을 호출하고 싶다 → 슬래시 커맨드
    │
    ├─ 특정 상황에서 자동으로 지식을 사용해주길 바란다 → 스킬
    │
    ├─ 복잡한 태스크를 분담시키고 싶다 → 서브에이전트
    │
    ├─ 툴 실행 전후에 자동 체크하고 싶다 → Hooks
    │
    ├─ 외부 서비스와 연계하고 싶다 → MCP
    │
    ├─ 출력 스타일을 바꾸고 싶다 → 출력 스타일
    │
    ├─ PR이나 issue 이벤트에서 자동으로 작업해주길 바란다 → GitHub Actions
    │
    └─ 팀 전체에 배포하고 싶다 → 마켓플레이스 (플러그인)
```

---

## **3.2 슬래시 커맨드:**

### **슬래시 커맨드란**

`.claude/commands/` 디렉터리에 Markdown 파일을 두는 것으로 정형 태스크를 `/커맨드명`으로 실행할 수 있게 하는 기능이다.

### **파일 구조:**

```
.claude/commands/
├── review.md          # /review
├── test.md            # /test
└── deploy-check.md    # /deploy-check
```

### **작성 예:**

```markdown
# 코드 리뷰
변경된 파일을 리뷰해줘. 다음 항목을 확인해:
- 버그나 로직 오류
- 타입 안전성
- 성능상의 문제점
- 코딩 규약 준수
```

### **$ARGUMENTS 변수:**

커맨드에 인수를 전달할 수 있다.

```markdown
# 파일 리뷰
다음 파일을 리뷰해줘: $ARGUMENTS
```

```
> /review src/api/user.ts
```

`$ARGUMENTS`에 `src/api/user.ts`가 대입되어 실행된다.

> 📎 출처: [Claude Code Docs - 슬래시 커맨드](https://code.claude.com/docs/ja/slash-commands)

---

## **3.3 스킬 (Skills):**

### **스킬이란**

특정 도메인에 관한 지식이나 처리를 Claude에게 자동으로 인식시키는 기능이다. 슬래시 커맨드와 달리, **Claude가 상황에 따라 자동으로 읽어들인다.**

### **배치 위치:**

```
.claude/skills/
├── security-audit.md    # 보안 관련 파일 조작 시에 읽어들임
├── performance.md       # 성능 최적화 지식
└── database.md          # DB 설계 지식
```

### **스킬 파일 예:**

```markdown
---
description: "TypeScript 타입 정의 파일을 편집할 때의 베스트 프랙티스"
---

# TypeScript 타입 정의 지식

## 중요한 패턴
- Utility Types (Partial, Required, Pick 등)를 적극적으로 활용한다
- any 타입은 절대 사용하지 않는다
- 제네릭을 사용하여 재사용성을 높인다
```

### **스킬 vs `CLAUDE.md`의 차이:**

| 항목 | `CLAUDE.md` | 스킬 |
|------|------------|------|
| 읽기 타이밍 | 항상 (세션 시작 시) | 관련 상황 시 자동 |
| 용도 | 전체 공통 규약 | 특정 도메인 지식 |
| 컨텍스트 소비 | 항상 소비 | 필요 시에만 소비 |

> 📎 출처: [Claude Code Docs - 스킬](https://code.claude.com/docs/ja/skills)

---

## **3.4 서브에이전트 (Sub-agents):**

### **서브에이전트란**

복잡한 태스크를 전문화된 에이전트에 위임하는 기능이다. 메인 Claude가 태스크를 판단하여 **자동으로 적절한 서브에이전트에 위임**한다.

### **배치 위치:**

```
.claude/agents/
├── code-reviewer.md    # 코드 리뷰 전문 에이전트
├── test-writer.md      # 테스트 작성 전문 에이전트
└── doc-writer.md       # 문서 작성 전문 에이전트
```

### **서브에이전트 파일 예:**

```markdown
---
description: "코드 리뷰를 전문으로 하는 에이전트. PR의 변경 내용을 리뷰할 때 호출된다."
---

당신은 시니어 엔지니어로서 코드 리뷰를 실시합니다.
다음 관점에서 리뷰하세요:
1. 버그나 로직 오류가 없는가
2. 성능상의 문제가 없는가
3. 보안상의 취약성이 없는가
4. 코드의 가독성과 유지보수성
```

### **스킬 vs 서브에이전트의 차이:**

| 항목 | 스킬 | 서브에이전트 |
|------|------|-----------|
| 역할 | 지식의 제공 | 태스크의 실행 |
| 컨텍스트 | 메인과 공유 | **독립된 컨텍스트** |
| 병렬 실행 | 불가 | **가능** |
| 적합한 용도 | 도메인 지식 제공 | 복잡한 태스크 분담 |

> 📎 출처: [Claude Code Docs - 서브에이전트](https://code.claude.com/docs/ja/sub-agents)

---

## **3.5 Hooks:**

### **Hooks란**

툴 실행 전후에 자동으로 스크립트를 실행하는 기능이다. 이벤트 구동으로 동작한다.

### **이벤트 종류:**

| 이벤트 | 타이밍 |
|--------|--------|
| `PreToolUse` | 툴 실행 전 |
| `PostToolUse` | 툴 실행 후 |
| `Notification` | 알림 발생 시 |
| `Stop` | Claude가 응답을 완료했을 때 |

### **설정 방법 (settings.json):**

```json
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "npm run lint --fix"
          }
        ]
      }
    ]
  }
}
```

이 설정에 의해, 파일 쓰기/편집 후 자동으로 lint가 실행된다.

### **활용 예:**

- 파일 편집 후 자동으로 포매터(Prettier 등) 실행
- 커밋 전 자동 테스트 실행
- 특정 파일 변경 시 알림

> 📎 출처: [Claude Code Docs - Hooks](https://code.claude.com/docs/ja/hooks)

---

## **3.6 MCP (Model Context Protocol):**

### **MCP란**

외부 서비스나 툴을 Claude Code에 연결하기 위한 프로토콜이다. **데이터베이스, GitHub, Slack 등 다양한 서비스와 통합**할 수 있다.

### **설정 방법:**

```json
// .mcp.json
{
  "mcpServers": {
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
      }
    }
  }
}
```

### **활용 예:**

- GitHub MCP: PR 생성, Issue 참조
- Slack MCP: 채널에 알림 전송
- DB MCP: 데이터베이스 직접 조회

> 📎 출처: [Claude Code Docs - MCP](https://code.claude.com/docs/ja/mcp)

---

## **3.7 출력 스타일:**

### **출력 스타일이란**

Claude의 응답 형식을 커스터마이즈하는 기능이다. `/output-style` 커맨드로 전환할 수 있다.

### **배치 위치:**

```
.claude/output-styles/
├── concise.md      # 간결한 응답
├── detailed.md     # 상세한 응답
└── japanese.md     # 일본어 응답
```

### **사용법:**

```
> /output-style concise
```

---

## **3.8 GitHub Actions 연계:**

### **GitHub Actions란**

PR이나 Issue 이벤트를 트리거로 Claude Code가 자동으로 작업하는 기능이다.

### **주요 활용 예:**

- PR 작성 시 자동 코드 리뷰
- Issue 생성 시 자동 구현
- 테스트 실패 시 자동 수정

### **설정 방법:**

```
> /install-github-app
```

이 커맨드로 GitHub App을 설치하고 리포지토리와 연결한다.

> 📎 출처: [Claude Code Docs - GitHub Actions](https://code.claude.com/docs/ja/github-actions)

---

## **3.9 마켓플레이스 (플러그인):**

### **플러그인이란**

슬래시 커맨드, 스킬, 서브에이전트 등을 **패키지화하여 팀이나 커뮤니티에 배포하는 기능**이다.

### **사용법:**

```
> /plugin install <plugin-name>
```

### **플러그인 작성:**

팀 내에서 독자적으로 작성하여 비공개로 공유하거나, 마켓플레이스에 공개 배포도 가능하다.

---

## **Part 4: 병렬 처리 (12분 + Q&A 2분)**

## **4.1 병렬 처리란?:**

여러 인스턴스의 Claude Code를 **동시에 실행**하는 기법이다. 독립적인 태스크를 병렬로 진행하여 전체 작업 시간을 단축할 수 있다.

```
직렬 실행:
태스크A → 태스크B → 태스크C   (합계 시간: 60분)

병렬 실행:
태스크A ─┐
태스크B ─┤ → 완료 (합계 시간: 20분)
태스크C ─┘
```

---

## **4.2 Git Worktree를 사용한 병렬 처리:**

### **Git Worktree란**

하나의 Git 리포지토리에서 **여러 워킹 디렉터리를 동시에 체크아웃**할 수 있는 기능이다. 이를 사용하면 충돌 없이 병렬로 작업할 수 있다.

### **사용법:**

```bash
# 새로운 워크트리 작성
git worktree add ../project-feature-a feature-a
git worktree add ../project-feature-b feature-b
git worktree add ../project-bugfix   bugfix

# 각 디렉터리에서 Claude Code를 기동
cd ../project-feature-a && claude
cd ../project-feature-b && claude
cd ../project-bugfix    && claude
```

### **병렬 처리 시의 컨텍스트 분리:**

각 워크트리는 독립된 디렉터리이므로, 각 Claude Code 인스턴스의 컨텍스트가 간섭하지 않는다.

```
project-feature-a/  ← Claude Code 인스턴스1
project-feature-b/  ← Claude Code 인스턴스2
project-bugfix/     ← Claude Code 인스턴스3
```

### **유스케이스:**

- 여러 피처를 동시에 개발
- 버그 수정과 피처 개발을 병행
- 리팩토링과 새 기능 추가를 동시에

> 📎 출처: [Claude Code 베스트 프랙티스](https://www.anthropic.com/engineering/claude-code-best-practices)

---

## **4.3 서브에이전트를 사용한 병렬 처리:**

### **오케스트레이션 패턴**

메인 Claude가 오케스트레이터(지휘자)가 되어, **여러 서브에이전트에 병렬로 태스크를 위임**하는 패턴이다.

```
메인 Claude (오케스트레이터)
    │
    ├─ 서브에이전트1: 프론트엔드 구현
    ├─ 서브에이전트2: 백엔드 API 구현
    └─ 서브에이전트3: 테스트 작성
```

각 서브에이전트는 **독립된 컨텍스트**에서 실행되므로, 메인 Claude의 컨텍스트를 압박하지 않고 병렬로 실행 가능하다.

### **프롬프트 예:**

```
여러 서브에이전트를 병렬로 사용하여 다음 태스크를 동시에 진행해줘:
1. 사용자 인증 API의 구현 (src/api/auth.ts)
2. 인증 화면의 UI 구현 (src/components/Auth.tsx)
3. 인증 로직의 테스트 작성 (src/__tests__/auth.test.ts)
```

> 📎 출처: [Claude Code Docs - 서브에이전트](https://code.claude.com/docs/ja/sub-agents)

---

## **4.4 병렬 처리 시의 주의점:**

- **파일 충돌**: 같은 파일을 여러 인스턴스가 동시에 편집하지 않도록 한다
- **의존 관계**: 태스크 간에 의존 관계가 있는 경우는 순서를 고려한다
- **컨텍스트 오염**: 서브에이전트는 독립된 컨텍스트이므로, 메인의 상세 지시는 전달해야 한다

---

## **Part 5: 피드백 루프 (10분 + Q&A 2분)**

## **5.1 피드백 루프란?:**

Claude Code의 출력에 대해 **자동으로 피드백을 받아, 품질을 반복적으로 향상시키는 구조**다.

```
Claude가 코드 생성
    ↓
테스트/Lint 자동 실행
    ↓
결과를 Claude에게 피드백
    ↓
Claude가 수정
    ↓
다시 테스트 실행...
```

---

## **5.2 테스트를 사용한 피드백 루프:**

### **기본 패턴:**

```
> 다음 함수를 구현해줘. 구현 후 반드시 `npm test`를 실행하여 테스트가 통과하는지 확인해줘. 테스트가 실패하면 수정해줘.
```

Claude는 구현 → 테스트 실행 → 실패하면 수정 → 재테스트 라는 사이클을 자동으로 반복한다.

### **`CLAUDE.md`에서의 설정:**

```markdown
# 개발 방침
- 구현 후에는 반드시 `npm test`를 실행하여 테스트 통과를 확인한다
- 테스트가 실패하면 원인을 분석하여 수정한다
- lint 오류가 있으면 반드시 수정하고 나서 완료로 한다
```

---

## **5.3 Lint/포매터를 사용한 피드백 루프:**

Hooks (3.5 참조)와 조합하면, 파일 편집 시 자동으로 Lint가 실행되어 결과가 Claude에게 피드백된다.

```json
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "npm run lint 2>&1 | head -50"
          }
        ]
      }
    ]
  }
}
```

Lint 결과가 자동으로 Claude의 컨텍스트에 추가되어, Claude가 즉시 수정하게 된다.

---

## **5.4 타입 체크를 사용한 피드백 루프:**

TypeScript 프로젝트에서는 타입 체크도 피드백으로 활용할 수 있다.

```
> 이 함수를 리팩토링해줘. `tsc --noEmit`을 실행하여 타입 오류가 없는지 확인해줘.
```

---

## **5.5 효율적인 피드백 루프 설계:**

- **빠른 피드백이 중요**: 피드백(테스트·Lint)이 빠를수록 사이클을 많이 돌릴 수 있다
- **피드백의 정밀도**: 오류 메시지는 가능한 한 구체적으로 Claude에게 전달한다
- **무한 루프 방지**: 최대 시도 횟수를 지시에 명기한다 ("3회 이상 실패하면 멈추고 상담해줘" 등)

---

## **Part 6: SDD (사양 주도 개발) (5분 + Q&A 2분)**

## **6.1 SDD란?:**

**Specification-Driven Development (사양 주도 개발)**. 코드를 쓰기 전에 사양을 문서로 정의하고, 그것을 바탕으로 Claude에게 구현시키는 개발 방법론이다.

```
기존 방식:
"○○ 기능을 구현해줘" → Claude가 마음대로 구현

SDD:
사양 문서 작성 → Claude가 사양대로 구현 → 사양과의 괴리를 체크
```

---

## **6.2 SDD의 흐름:**

1. **사양 정의**: 요건, 인터페이스, 동작, 엣지 케이스 등을 문서화
2. **사양 리뷰**: 사양 문서의 모순이나 빠뜨린 항목을 체크 (Claude에게 질문시키는 것도 유효)
3. **구현 지시**: 사양 문서를 컨텍스트로 전달하여 Claude에게 구현시킨다
4. **검증**: 구현이 사양을 충족하는지 테스트로 확인

---

## **6.3 사양 문서의 예:**

```markdown
# 사용자 인증 API 사양

## 엔드포인트
POST /api/auth/login

## 요청
- email: string (필수, 이메일 형식)
- password: string (필수, 8자 이상)

## 응답 (성공)
- status: 200
- body: { token: string, user: { id, email, name } }

## 응답 (실패)
- 400: 입력값 오류 (email 형식 이상, password 조건 미달)
- 401: 인증 실패 (이메일/비밀번호 불일치)
- 500: 서버 오류

## 엣지 케이스
- 연속 5회 실패 시 계정 잠금 (30분간)
- 삭제된 계정에서의 로그인은 401
```

---

## **6.4 SDD의 장점:**

- **의도하지 않은 구현을 방지**: Claude가 마음대로 구현하는 것을 방지한다
- **레뷰가 쉬워진다**: 사양 vs 구현으로 비교 확인이 가능
- **테스트 설계가 쉬워진다**: 사양이 그대로 테스트 케이스가 된다
- **팀 공유가 쉬워진다**: 사양 문서가 공유 지식이 된다

---

## **6.5 `CLAUDE.md`와의 연계:**

```markdown
# 개발 방침
- 새 기능 구현 전에 반드시 사양 문서를 `docs/specs/`에 작성한다
- 사양 문서 없이 구현을 시작하지 않는다
- 사양이 애매한 경우는 AskUserQuestion으로 확인한다
```

---

## **Part 7: Team 플랜 환경 (10분 + Q&A 2분)**

## **7.1 Team 플랜이란?:**

조직 전체에서 Claude Code를 관리·운용하기 위한 플랜이다. 개인의 Max 플랜과 달리, **관리자가 정책을 설정하거나 사용 현황을 모니터링**할 수 있다.

---

## **7.2 Team 플랜의 특징:**

| 항목 | 개인 플랜 | Team 플랜 |
|------|---------|---------|
| 관리자 콘솔 | 없음 | 있음 |
| 사용 현황 모니터링 | 없음 | 있음 |
| 정책 설정 | 없음 | 있음 |
| 엔터프라이즈 설정 | 없음 | 있음 |
| SSO (싱글 사인온) | 없음 | 있음 |

---

## **7.3 엔터프라이즈 설정:**

관리자가 조직 전체에 적용하는 정책을 설정할 수 있다.

- 허가하는 툴의 범위
- 외부 서비스 연계의 허가/비허가
- 사용할 수 있는 모델의 제한
- 로깅 설정

---

## **7.4 플러그인을 사용한 팀 표준화:**

팀 내에서 공통으로 사용하는 커맨드나 스킬을 플러그인으로 패키지화하여 공유할 수 있다.

```
팀 공통 플러그인:
- 코드 리뷰 커맨드
- 테스트 작성 커맨드
- 사양 문서 템플릿 생성
- 팀 고유의 코딩 규약 스킬
```

### **도입 흐름:**

1. 팀 관리자가 플러그인을 작성·등록
2. 팀 멤버가 `/plugin install team-standard` 로 설치
3. 팀 전체에서 동일한 커맨드·스킬을 사용 가능

---

## **7.5 Bedrock 연계:**

AWS Bedrock을 통해 Claude Code를 종량 과금으로 사용할 수도 있다. 다음과 같은 경우에 적합하다.

- 엔터프라이즈 보안 요건이 있는 경우
- 사용량을 세밀하게 제어하고 싶은 경우
- AWS 인프라와 통합하고 싶은 경우

```bash
# Bedrock을 사용하기 위한 환경 변수 설정
export ANTHROPIC_API_KEY=""
export AWS_BEDROCK_REGION="us-east-1"
export CLAUDE_CODE_USE_BEDROCK=true
```

> 📎 출처: [Claude Code Docs - Bedrock 설정](https://code.claude.com/docs/ja/amazon-bedrock)

---

## **Part 8: 정리 (5분)**

## **8.1 오늘의 정리:**

**Part 1-2 (기초·컨텍스트 운용)**
- `CLAUDE.md`는 프로젝트 고유의 지시를 작성한다
- `rules/`로 룰을 분리하고 `paths`를 지정하여 컨텍스트를 효율화한다
- 토큰은 "눈덩이식"으로 증가하므로 `/clear`·`/compact`·새 세션을 구분하여 사용한다
- Plan 모드는 복잡한 태스크에서 사용하고, "Yes, clear context..." 를 선택한다

**Part 3 (확장 기능)**
- 슬래시 커맨드: 정형 태스크를 호출한다
- 스킬: 특정 도메인의 지식을 자동으로 활용시킨다
- 서브에이전트: 복잡한 태스크를 분담·병렬화한다
- Hooks: 툴 실행 전후에 자동 체크를 넣는다
- MCP: 외부 서비스와 연계한다

**Part 4 (병렬 처리)**
- Git Worktree로 여러 인스턴스를 동시에 실행한다
- 서브에이전트로 독립된 컨텍스트에서 병렬 실행한다

**Part 5 (피드백 루프)**
- 테스트·Lint의 결과를 자동으로 피드백하여 품질을 반복적으로 향상시킨다

**Part 6 (SDD)**
- 코드 전에 사양을 정의하고, Claude에게 사양대로 구현시킨다

**Part 7 (Team 플랜)**
- 관리자 콘솔로 조직 전체를 관리한다
- 플러그인으로 팀 표준을 공유한다

---

## **8.2 실천을 위한 다음 한 걸음:**

- **오늘 할 것**: `CLAUDE.md`를 검토하고 불필요한 내용을 `rules/`로 분리한다
- **이번 주 할 것**: 슬래시 커맨드를 하나 만들어보고 정형 작업을 자동화한다
- **이번 달 할 것**: 팀에서 공통으로 사용하는 스킬이나 커맨드를 플러그인화한다

---

## **8.3 참고 링크:**

- [Claude Code 공식 문서](https://code.claude.com/docs/ja)
- [Claude Code 베스트 프랙티스](https://code.claude.com/docs/en/best-practices)
- [Claude Code 베스트 프랙티스 (Anthropic Engineering Blog)](https://www.anthropic.com/engineering/claude-code-best-practices)  