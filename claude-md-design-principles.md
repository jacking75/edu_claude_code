 # CLAUDE.md는 길수록 효과적인 것이 아니다. 오히려 길수록 Claude Code는 따르기 어려워진다.
[출처](https://qiita.com/nogataka/items/d6c83ea50b82e1c2602c)  
  
100줄짜리 CLAUDE.md와, 35줄로 줄이고 나머지를 `.claude/rules/`로 분리한 CLAUDE.md. 동일한 지시를 내려도 후자 쪽이 Claude의 출력 품질이 명백히 높다.

이 글에서는 왜 그런지를 공식 사양에 기반해 설명하고, "어느 줄을 남기고, 어느 줄을 옮기고, 어느 줄을 지울 것인가"를 1줄 단위로 설계하는 방법론을 소개한다.

---

**CLAUDE.md의 주입 메커니즘 — 왜 "묻히는가":**

**User Message로 주입된다는 사실**

공식 문서에는 이렇게 적혀 있다.

> CLAUDE.md adds the contents as a user message following Claude Code's default system prompt.
> — [Claude Code Memory Documentation](https://docs.anthropic.com/en/docs/claude-code/memory)

CLAUDE.md는 System Prompt의 일부가 아니라, default system prompt 뒤에 **User Message로 추가**된다. 시작 시에는 CLAUDE.md 외에도, auto memory, MCP tool names, skills 목록 등 복수의 startup context가 읽혀진다.

이 "User Message다"라는 사실이 효과 감쇠의 근본 원인이다.

**4가지 감쇠 메커니즘**

CLAUDE.md의 효과가 옅어지는 것은 단순히 "오래됐기 때문"이 아니다. 4가지 메커니즘이 복합적으로 작용한다.

**1. 위치 바이어스 (Recency Bias)**
LLM은 모든 토큰을 균등하게 다루지 않는다. 가장 최근 발화에 가중치가 쏠린다. 50턴 후의 CLAUDE.md는, 최신 사용자 지시에 대해 상대적으로 약한 존재가 된다.

**2. Attention 희석**
컨텍스트 내 토큰 수가 늘어날수록, 각 토큰에 할당되는 Attention은 분산된다. CLAUDE.md의 30줄에 쏟아지던 주의가, 수천 토큰의 대화 이력에 희석된다.

**3. 명령의 경합 (Instruction Drift)**
대화 중에 "이번엔 테스트 생략해줘", "이 부분만 예외로" 같은 일시적 지시가 쌓이면, CLAUDE.md의 원래 방침과 경합한다. LLM은 최근 지시를 우선하기 때문에, 원래 규칙이 실질적으로 덮어씌워진다.

**4. 요약·압축의 영향**
Claude Code는 긴 대화를 내부적으로 압축(compact)한다. 이 과정에서 CLAUDE.md의 내용은 요약되어, 세세한 뉘앙스가 소실될 가능성이 있다.

**턴 수와 효과의 관계**

실제 운용에서의 관측에 기반한 기준이다. 프로젝트의 복잡성이나 CLAUDE.md의 줄 수에 따라 달라진다.

| 턴 수 | 상태 | 대책 |
|--------|------|------|
| ~10 | 거의 유지됨. CLAUDE.md 지시대로 동작한다 | 특별히 없음 |
| 10~30 | 경미한 흔들림. 세세한 규칙이 지켜지지 않는 경우가 있다 | 중요한 규칙은 대화 중 재제시 |
| 30+ | 명확하게 저하됨. 행동 원칙 수준의 지시도 무시될 수 있다 | 세션 분할 or `/compact` 후 재확인 |

공식 문서에서는 "CLAUDE.md files should be under 500 lines per file"라고 하면서도, 실천적으로는 짧을수록 adherence(준수도)가 높다고 시사하고 있다. 이 글에서는 이 성질을 "주의 자원의 배분"으로 정리한다.

---

**CLAUDE.md의 모든 줄에는 주의 비용이 있다:**

CLAUDE.md의 각 줄은, Claude의 한정된 주의 자원을 소비한다. 줄 수 자체보다도, **각 줄이 얼마나 "판단 개선"을 가져오는가**가 중요하다.

**주의 비용에 걸맞는 줄**

```
## 기술 스택
- Next.js 15 (App Router)          ← 효과: 매우 높음. Pages Router 코드 생성을 방지
- TypeScript 5.7 (strict mode)     ← 효과: 높음. any를 피하고, 타입 안전한 코드가 된다
```

기술 스택의 **버전 지정**은, 1줄당 효과가 가장 높은 기술이다. "Next.js 15"라고 쓰는 것만으로, Claude는 App Router, Server Components, Server Actions를 전제로 한 코드를 생성한다.

**주의 비용에 걸맞지 않는 줄**

```
## 코딩 규약
- 들여쓰기는 2스페이스              ← 효과: 낮음. Prettier로 자동 수정됨
- 세미콜론을 붙인다                 ← 효과: 낮음. ESLint로 자동 수정됨
- import문은 알파벳 순으로 정렬한다  ← 효과: 낮음. eslint-plugin-import로 자동 수정됨
```

Linter나 포매터로 강제할 수 있는 규칙은, CLAUDE.md에 쓰는 우선순위가 낮다. Claude가 규약에 맞지 않는 코드를 써도 저장 시 자동 수정되므로, 귀중한 주의 자원을 쓸 필요가 없다.

공식 문서에는 `Use 2-space indentation` 같은 예시도 게재되어 있다. Linter 미도입 프로젝트에서는 유효하지만, Linter가 동작하는 환경에서는 더 중요한 지시에 줄을 쓰는 것이 효과적이다.

**가장 효과가 높은 줄 (간과되기 쉬운 것)**

```
## 행동 원칙
- 3스텝 이상의 태스크는 반드시 Plan 모드로 시작한다
```

이 1줄의 효과는 헤아릴 수 없을 정도다. Claude가 복잡한 태스크에 대뜸 코드를 쓰기 시작해 도중에 파탄 나는 문제를 근본부터 방지한다.

```
- 코드를 읽지 않고 쓰지 않는다. 반드시 기존 코드를 확인하고 나서 변경한다
```

이것도 마찬가지다. Claude가 기존 구현을 무시하고 다른 접근으로 재작성해버리는 문제를 방지한다.

**기술 스택이나 규약보다, 행동 원칙 쪽이 주의 비용당 효과가 높다**. 이것이 실제 운용에서 얻은 가장 중요한 지견이다.

---

**CLAUDE.md에 써야 할 줄, 쓰지 말아야 할 줄:**

**써야 할 것 (CLAUDE.md에 남긴다)**

| 섹션 | 효과 | 이유 |
|------|------|------|
| 프로젝트 개요 (1-2줄) | 높음 | Claude의 모든 행동의 컨텍스트를 결정한다 |
| 기술 스택 (버전 포함) | 매우 높음 | 생성 코드의 API 선택을 직접 제어한다 |
| 커맨드 목록 | 높음 | 테스트·빌드 실행의 정확성과 직결된다 |
| 디렉터리 구조 | 높음 | 파일 배치의 정확성과 직결된다 |
| 행동 원칙 | 매우 높음 | Claude의 사고 프로세스 자체를 제어한다 |

**쓰지 말아야 할 것 (`.claude/rules/`로 이동)**

| 섹션 | 효과 | 이유 |
|------|------|------|
| 코드 스타일 상세 | 낮음 | Linter/Formatter로 강제 가능 |
| 테스트 규약 | 중간 | 테스트 파일 작업 시에만 필요 |
| 보안 규칙 | 중간 | 항상 필요하지만 상세하면 비대해짐 |
| Git/커밋 규약 | 낮음 | 커밋 시에만 필요 |
| 에러 핸들링 규약 | 중간 | 코드 작업 시에만 필요 |

**쓰지 말아야 할 것 (어디에도 쓰지 않는다)**

| 내용 | 이유 |
|------|------|
| 오래된 코드 스니펫 | 코드는 변한다. CLAUDE.md에 붙여넣은 스니펫은 1주일이면 썩는다 |
| 일반적인 베스트 프랙티스 | "DRY하게 쓴다", "SOLID에 따른다"는 Claude가 이미 알고 있다 |
| README 내용을 복붙한 것 | README는 인간용이다. Claude에게 필요한 정보는 다르다 |

---

**Conditional Rules — 세션 후반에도 효과가 있는 구조:**

CLAUDE.md에서 분리한 규칙의 수용처가 `.claude/rules/`다.

**왜 CLAUDE.md보다 효과적인가**

`.claude/rules/`의 파일은, 매칭되는 파일을 Claude가 다룰 때 컨텍스트에 주입된다.

빨간 CLAUDE.md는 50메시지 전에 묻혀 있지만, 초록 Conditional Rules는 필요할 때 가장 최근 위치에 주입된다. 세션 후반에도 높은 효과를 발휘한다.

**경로 지정의 실천 예**

`.claude/rules/`에서는 frontmatter의 `paths` 필드로 규칙의 적용 대상을 지정할 수 있다.

`paths`는 쉼표 구분(CSV 형식)으로 기술한다. YAML 배열 형식(`- "..."` 리스트)은 내부 파서 버그로 인해 올바르게 처리되지 않는다 ([수정 PR](https://github.com/icew4y/everything-claude-code/pull/1)).

```markdown
---
paths: src/api/**/*.ts,src/routes/**/*.ts
---

# API 밸리데이션 규약

- 모든 엔드포인트에 Zod 스키마로 밸리데이션을 구현한다
- 요청 바디, 쿼리 파라미터, 경로 파라미터 모두가 대상
- 밸리데이션 에러는 422로 반환하고, 에러 상세를 응답에 포함한다
```

```markdown
---
paths: src/components/**/*.tsx,src/app/**/page.tsx
---

# React 컴포넌트 규약

- Server Components를 기본으로 한다. "use client"는 최소한으로
- Props는 interface로 정의하고, 컴포넌트 바로 위에 배치한다
- children 이외의 렌더링 Props는 피한다
```

```markdown
---
paths: "**/*.test.ts,**/*.test.tsx,**/*.spec.ts"
---

# 테스트 규약

- 테스트 이름은 "~한 경우, ~가 된다" 형식
- 모킹은 최소한으로. 실제 DB/API를 사용하는 integration test를 우선
- 테스트 데이터는 팩토리 함수로 생성한다
```

이 구성으로, Claude가 컴포넌트를 쓸 때는 React 규약이, 테스트를 쓸 때는 테스트 규약이, 각각 최적의 타이밍에 주입된다.

---

**행동 원칙의 설계 — Claude의 사고 프로세스를 제어한다:**

기술 스택은 "무엇을 만드는가"를 제어하지만, 행동 원칙은 "어떻게 만드는가"를 제어한다. 주의 비용 분석에서 가장 효과가 높은 섹션이다.

**제어해야 할 3가지 행동**

**1. 계획을 세우게 한다**

```
- 3스텝 이상의 태스크는 반드시 Plan 모드로 시작한다
- 플랜에는 의도(왜 필요한가)와 선택 이유를 포함한다
- 도중에 잘 안 될 경우, 무리하게 진행하지 말고 멈춰서 재계획한다
```

Claude는 계획 없이 코드를 쓰기 시작하는 경향이 있다. 복잡한 태스크에서 이런 일이 생기면, 도중에 방침이 파탄 나고 대량의 재작업이 발생한다.

**2. 검증을 강제한다**

```
- 동작을 증명할 수 있을 때까지 태스크를 완료로 표시하지 않는다
- 테스트를 실행하고, 로그를 확인하고, 올바르게 동작함을 보여준다
```

"됐습니다"라는 말을 듣고 확인해보니 에러였던 경험은 누구에게나 있을 것이다. 검증의 강제는, 이 재작업 비용을 극적으로 줄인다.

**3. 컨텍스트의 자기 관리를 요구한다**

```
- 코드를 읽지 않고 쓰지 않는다. 반드시 기존 코드를 확인하고 나서 변경한다
- 컨텍스트가 얼마 남지 않은 경우, 그 사실을 전하고 구분짓기를 제안한다
- 리서치·조사는 서브에이전트에 위임해 메인 컨텍스트를 절약한다
```

컨텍스트 윈도우가 빡빡해지면, Claude는 품질을 희생해 작업을 서두르는 경향이 있다. 품질을 떨어뜨릴 바에는 멈추는 것을 명시하는 규칙이다.

**Before / After — 설정뿐만 아니라, 행동이 어떻게 달라지는가**

Before: 행동 원칙 없음 (50줄짜리 CLAUDE.md)

```markdown
# MyApp

## 기술 스택
- Next.js 15 (App Router)
- TypeScript 5.7
- Tailwind CSS v4

## 커맨드
- npm run dev
- npm run build
- npm test

## 코딩 규약
- 2스페이스 들여쓰기
- 세미콜론 없음
- 싱글 쿼트
- 파일명은 kebab-case
- 컴포넌트는 PascalCase
- 변수명은 camelCase
- const 우선
- 타입 어노테이션 필수
- any 금지
- interface 우선
- default export 금지 (페이지 제외)
... (이하, 코드 스타일이 30줄 계속됨)
```

문제점은 3가지다.

1. 코드 스타일에 30줄을 쓰고, 행동 원칙이 제로
2. Linter로 강제할 수 있는 내용이 대부분
3. Claude의 "어떻게 일할 것인가"가 미정의

이 CLAUDE.md로 "유저 인증 기능을 추가해줘"라고 의뢰하면, Claude는 계획 없이 대뜸 코드를 쓰기 시작하고, 도중에 설계가 파탄 나, 3번의 재작업이 발생했다.

After: 주의 자원을 최적 배분 (35줄짜리 CLAUDE.md + 3개의 rules 파일)

```markdown
# MyApp

React + TypeScript로 구축된 태스크 관리 앱. Supabase를 백엔드로 사용.

## 기술 스택
- Next.js 15 (App Router)
- TypeScript 5.7 (strict mode)
- Tailwind CSS v4 + shadcn/ui
- Supabase (PostgreSQL + Auth + Storage)

## 커맨드
| 커맨드 | 용도 |
|--------|------|
| `npm run dev` | 개발 서버 |
| `npm run build` | 빌드 |
| `npm test` | Vitest |
| `npm run e2e` | Playwright |

## 디렉터리 구조
| 경로 | 역할 |
|------|------|
| `src/app/` | 페이지·레이아웃 |
| `src/components/` | UI 컴포넌트 |
| `src/lib/` | 유틸리티 |
| `src/types/` | 타입 정의 |

## 행동 원칙
- 3스텝 이상의 태스크는 반드시 Plan 모드로 시작한다
- 동작을 증명할 수 있을 때까지 태스크를 완료로 표시하지 않는다
- 코드를 읽지 않고 쓰지 않는다
- 변경은 필요한 곳만. 영향 범위를 최소화한다
- 컨텍스트가 빡빡해지면 솔직하게 전한다
```

코드 스타일은 `.claude/rules/code-style.md`, 테스트 규약은 `.claude/rules/testing.md`, 보안은 `.claude/rules/security.md`에 각각 분리.

동일한 "유저 인증 기능을 추가해줘" 의뢰에 대해, Claude는 먼저 계획을 제시하고, 승인을 받은 뒤 구현에 들어가, 테스트가 통과함을 확인하고 나서 완료를 보고했다. 재작업은 제로였다.

After 쪽이 줄 수는 적지만, 행동 원칙의 5줄이 Claude의 사고 프로세스를 직접 제어하고 있다.

---

**퍼스널리티 설계 — "어떤 Claude와 일할 것인가"를 정의한다:**

행동 원칙 중에서도 특히 효과가 높은 것이, 퍼스널리티와 워크스타일의 지정이다.

동일한 "React 컴포넌트를 만들어줘"라는 지시라도, 퍼스널리티에 따라 출력이 달라진다.

| 퍼스널리티 | 출력의 경향 |
|------------|------------|
| 미니멀리스트 | 최소한의 Props, 최소한의 state. 불필요한 추상화를 하지 않는다 |
| 설계 품질 중시 | 엣지 케이스를 고려한 타입 설계. "더 좋은 방법은 없는가"를 자문한다 |
| 자율형 | 주변의 타입 정의나 유틸리티도 자발적으로 생성. 보고는 결과만 간결하게 |
| 함께하는 형 | "이 접근으로 괜찮으세요?"라고 확인하면서 단계적으로 진행한다 |

어느 것이 정답이라는 것은 아니고, 프로젝트의 페이즈나 작업 내용에 따라 최적해가 달라진다.

| 상황 | 추천 조합 | 이유 |
|------|-----------|------|
| 프로토타입 단계 | 미니멀리스트 + 자율형 | 스피드 우선. 불필요한 확인을 줄인다 |
| 본번 코드 | 설계 품질 중시 + 신중형 | 품질 우선. 파괴적 변경 전에 확인을 받는다 |
| 처음 다루는 프레임워크 | 멘토형 + 함께하는 형 | 학습 우선. 왜 그렇게 하는지 설명해 받는다 |
| 리뷰 의뢰 | 논리적 + 팩트체커 | 정확성 우선. 근거를 제시하고 사실 확인을 한다 |

**실제 지정 예**

```markdown
## 행동 원칙

### 퍼스널리티: 논리적 + 미니멀리스트
- 결론 먼저 전달한다
- 변경의 근거를 반드시 제시한다
- 설명은 최소한으로. 코드로 말한다
- 불필요한 추상화·과잉 설계를 피한다

### 워크스타일: 검증 중시 + 컨텍스트 관리
- 동작을 증명할 수 있을 때까지 완료로 하지 않는다
- 테스트 실행·빌드 확인을 생략하지 않는다
- 컨텍스트가 빡빡해지면 솔직하게 전하고, 구분짓기를 제안한다
- 리서치는 서브에이전트에 위임한다
```

이 조합은 "논리적이고 낭비 없는 구현을 하면서, 품질 보증은 손을 빼지 않는" Claude를 만들어낸다.

---

**복붙용: 최소 CLAUDE.md 템플릿:**

지금까지의 지견을 응축한, 바로 쓸 수 있는 템플릿이다. `{...}` 부분을 자신의 프로젝트에 맞게 바꿔 쓰면 된다.

```markdown
# {프로젝트명}

{1-2줄의 프로젝트 개요}

## 기술 스택
- {프레임워크 + 버전}
- {언어 + 버전}
- {주요 라이브러리}

## 커맨드
| 커맨드 | 용도 |
|--------|------|
| `{dev command}` | 개발 서버 |
| `{build command}` | 빌드 |
| `{test command}` | 테스트 |
| `{lint command}` | Lint |

## 디렉터리 구조
| 경로 | 역할 |
|------|------|
| `{src dir}` | {역할} |
| `{components dir}` | {역할} |
| `{lib dir}` | {역할} |

## 행동 원칙
- 3스텝 이상의 태스크는 반드시 Plan 모드로 시작한다
- 동작을 증명할 수 있을 때까지 태스크를 완료로 표시하지 않는다
- 코드를 읽지 않고 쓰지 않는다
- 변경은 필요한 곳만. 영향 범위를 최소화한다
- 컨텍스트가 빡빡해지면 솔직하게 전한다
```

이 템플릿은 약 30줄이다. 여기에 자신의 프로젝트 정보를 채우고, 퍼스널리티나 워크스타일의 행동 원칙을 추가하는 것만으로, 효과적인 CLAUDE.md가 완성된다.

템플릿을 처음부터 쓰는 것이 귀찮은 경우에는, 40개 이상의 언어·프레임워크에 대응한 템플릿과 커스텀 빌더를 준비하고 있다.

---

**CLAUDE.md 빌더 — 이 글의 설계 원칙을 툴로 만들었다:**

지금까지 설명한 설계 원칙을, 매번 수작업으로 적용하는 것은 현실적이지 않다.

그래서, 이 방법론을 툴화한 **CLAUDE.md 빌더**를 만들었다.

**빌더 (커스텀 생성)**

8가지 스텝을 선택하는 것만으로, 주의 자원을 최적 배분한 CLAUDE.md를 즉시 생성한다.

| 스텝 | 선택 내용 |
|------|----------|
| 프로젝트 개요 | 이름과 개요문 (임의) |
| 베이스 | 40+ 종의 언어/FW (Frontend, Backend, Mobile, Infra, Data/AI, Game, Embedded) |
| 유스케이스 | 개인 개발 / 팀 / OSS / 엔터프라이즈 / 프로토타입 / 학습 |
| 레벨 | 초심자 / 중급자 / 상급자 |
| 역할 | 테크리드 / 파트너 / 리뷰어 등 (복수 가능) |
| 퍼스널리티 | 논리적 / 미니멀리스트 / 멘토 등 (복수 가능) |
| 워크스타일 | 검증 중시 / 신중 / 위임형 등 (복수 가능) |
| 톤 | 정중 / 캐주얼 / 간결 / 상세 |

오른쪽 프리뷰에 실시간으로 반영되므로, 선택하면서 확인할 수 있다.

템플릿을 그대로 쓰고 싶은 경우에는, 템플릿 목록에서 검색·필터해 원클릭으로 복사할 수 있다. CLAUDE.md의 작성법을 체계적으로 배우고 싶은 경우에는, 해설 가이드도 준비되어 있다.

---

**정리 — CLAUDE.md의 어디에 쓸지 체크리스트:**

| 판단 기준 | 배치처 |
|-----------|--------|
| 세션 시작 직후에 필요 | **CLAUDE.md** |
| 특정 파일 작업 시에만 필요 | **.claude/rules/** (paths 지정) |
| Linter/Formatter로 자동 수정 가능 | **쓰지 않는다** |
| 태스크 고유의 절차 | **skills** |

CLAUDE.md의 설계 원칙을 한마디로 정리하면, **"적게 쓰고, 많이 제어한다"**이다.

100줄짜리 CLAUDE.md를 35줄로 줄이고, 줄인 분량을 `.claude/rules/`로 이동한다. 그것만으로 Claude의 출력 품질은 달라진다.  